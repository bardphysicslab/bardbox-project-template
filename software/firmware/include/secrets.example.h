#pragma once

// Copy to secrets.h, then set your network credentials. This example firmware
// enables Wi-Fi recovery only when both macros are defined.
#define BARDBOX_WIFI_SSID "YOUR_WIFI_SSID"
#define BARDBOX_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
